{
  "name": "@org/web",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "projectType": "application",
    "tags": ["scope:shop"],
    "targets": {
      "serve": {
        "dependsOn": ["@org/api:serve"]
      },
      "preview": {
        "dependsOn": ["build", "@org/api:serve"]
      }
    }
  },
  "devDependencies": {
    "@org/models": "workspace:*"
  }
}
